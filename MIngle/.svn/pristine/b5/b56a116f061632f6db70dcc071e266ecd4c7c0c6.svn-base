package scripts.iondesk;

import java.util.HashMap;

import org.testng.annotations.AfterClass;
import org.testng.annotations.BeforeClass;

import com.relevantcodes.extentreports.ExtentTest;

import actions.Actions;
import objRepository.Global;
import reusableMethods.RM_IONDESK;
import utility.Utility;

public class Verify_AssignOtheruser
{
	ExtentTest logger;
	private Utility utl;
	 HashMap<String, String> testData=Utility.getTestData();
	 HashMap<String, String> configData=Utility.getConfigData();
		
		//TESTDATA
		 String userName=configData.get("USER12");
		 String userName1=configData.get("USER14");
		 String pwd=configData.get("PASSWORD");
		 String wfname=testData.get("WORKFLOW_NAME");
		 String user=testData.get("WORKFLOW_USER1");
		 
	@BeforeClass
	public void setUp()
	{
		 utl=new Utility();
		 logger=utl.initTest(this.getClass().getName().substring(this.getClass().getName().lastIndexOf(".")+1).trim());
		 new Actions();
	
	}
	@AfterClass
	public void tearDown()
	{
		Utility.report.endTest(logger);
		//Actions.driverClose();
	
	}
	


	@org.testng.annotations.Test(priority =1)
	public void start_WorkFlowOtherUser1()
	 {
		try {
			
	    //login
		Global.login(userName,pwd);
		RM_IONDESK.start_WorkFlowOtherUser(wfname,user);
		 
         } catch (Exception e) {
			
		}finally
		{
			Global.logOut();
			Actions.driverClose();
		}
		
     }
	
	@org.testng.annotations.Test(priority =2)
	public void start_WorkFlowOtherUser2()
	 {
		try {
			
	    //login
		Global.login(userName1,pwd);
		RM_IONDESK.complete_WorkFlowOtherUser(wfname,user);
		 
         } catch (Exception e) {
			
		}finally
		{
			Global.logOut();
			Actions.driverClose();
		}
		
     }
	
	@org.testng.annotations.Test(priority =3)
	public void remove_WorkFlowOtherUser1()
	 {
		try {
			
	    //login
		Global.login(userName,pwd);
		RM_IONDESK.remove_WorkFlowOtherUser(wfname);
		 
         } catch (Exception e) {
			
		}finally
		{
			Global.logOut();
			Actions.driverClose();
		}
		
     }
}
