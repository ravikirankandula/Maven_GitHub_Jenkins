/**
 * 
 */
package scripts.IFS;

import java.io.IOException;
import java.util.HashMap;


import org.testng.annotations.AfterClass;
import org.testng.annotations.BeforeClass;

import com.relevantcodes.extentreports.ExtentTest;

import actions.Actions;
import objRepository.Global;
import reusableMethods.RM_IFS;
import utility.Utility;


public class Ts_ServiceAccounts_13292 {
	ExtentTest logger;
	private Utility utl;
	 HashMap<String, String> testData=Utility.getTestData();
	 HashMap<String, String> configData=Utility.getConfigData();
		
		//TESTDATA
		 String userName=configData.get("USER1");	
		 String pwd=configData.get("PASSWORD");
		 String desc=testData.get("SERVICEACCOUNTDESC");
		 String serviceUser=testData.get("SERVICEACCOUNTDESC");
	
	@BeforeClass
	public void setUp()
	{
		 utl=new Utility();
		 logger=utl.initTest(this.getClass().getName().substring(this.getClass().getName().lastIndexOf(".")+1).trim());
		 new Actions();
	
	}
	@AfterClass
	public void tearDown() throws Exception
	{
		Utility.report.endTest(logger);
		//Actions.driverClose();
		//SendEmail.zipReport();
		//SendEmail.execute("./zipdemo.zip");
		//SendEmail.execute("./zipdemo.zip");
	}
	


	@org.testng.annotations.Test(priority =1)
	public void create_ServiceAccount(){
		try {
			
			//LOGIN
			Global.login(userName,pwd);
			RM_IFS.serviceAccounts(desc);
			
		} catch (Exception e) {
			
		}finally
		{
			Global.logOut();
			Actions.driverClose();
			rb_ServiceAccount();
		}
	}
	//@org.testng.annotations.Test(priority =2, dependsOnMethods ="create_ServiceAccount")
	public void rb_ServiceAccount()
	{
		try {
			Global.login(userName,pwd);
		RM_IFS.rb_serviceAccounts(desc,serviceUser);
			
		} catch (Exception e) {
			
		}
		finally
		{
			Global.logOut();
			Actions.driverClose();
		}
	}
	
}
