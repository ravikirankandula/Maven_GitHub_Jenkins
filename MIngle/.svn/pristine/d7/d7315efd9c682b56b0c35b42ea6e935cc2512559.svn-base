package scripts.IFS;

import java.util.HashMap;

import org.testng.annotations.AfterClass;
import org.testng.annotations.BeforeClass;

import com.relevantcodes.extentreports.ExtentTest;

import actions.Actions;
import objRepository.Global;
import reusableMethods.RM_IFS;
import utility.Utility;

public class Ts_AddContactsToContactGroup_13304
{
	ExtentTest logger;
	private Utility utl;
	 HashMap<String, String> testData=Utility.getTestData();
	 HashMap<String, String> configData=Utility.getConfigData();
		
		//TESTDATA
		 String userName=configData.get("USER1");
		 String pwd=configData.get("PASSWORD");
		 String email=configData.get("USER7");
		 String fullName=testData.get("CONTACT_FULLNAME");
		 String contactID=testData.get("CONTACT_ID");
		 String groupname=testData.get("CONTACT_GROUPNAME");
		 String desc=testData.get("CONTACT_GROUPDESC");
	
	
	@BeforeClass
	public void setUp()
	{
		 utl=new Utility();
		 logger=utl.initTest(this.getClass().getName().substring(this.getClass().getName().lastIndexOf(".")+1).trim());
		 new Actions();
	
	}
	@AfterClass
	public void tearDown()
	{
		Utility.report.endTest(logger);
		//Actions.driverClose();
	
	}
	


	@org.testng.annotations.Test(priority =1)
	public void AddContactsToContactGroup()
	 {
		try {
			
	    //login
		Global.login(userName,pwd);
		RM_IFS.addcontacts_ToContactGrps(groupname,desc,fullName,email,contactID);
		
         } catch (Exception e) {
			
		}finally
		{
			Global.logOut();
			Actions.driverClose();
			rb_Contacts_Grps();
		}
		
     }
	//@org.testng.annotations.Test(priority =2, dependsOnMethods ="dgImport_Export")
		public void rb_Contacts_Grps()
		{
			try {
				 Global.login(userName,pwd);
				 RM_IFS.rb_AddContactstoGrp(fullName,email,contactID,groupname);
				 //RM_IFS.delete_ContactGroups();
			} catch (Exception e) {
				
			}
			finally
			{
				Global.logOut();
				Actions.driverClose();
			}
		}
}
